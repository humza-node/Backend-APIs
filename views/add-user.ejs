<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Data</title>
    <style>
       
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        form {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 300px; /* Adjust the width as needed */
        }

        div {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
        }

        li {
            list-style-type: none;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
        }

        label {
            margin-right: 10px;
        }

        button {
            background-color: #4caf50;
            color: #fff;
            padding: 12px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }

        /* Additional styles for your input fields, select, etc. */
        input, select {
            width: 100%; /* Make the input fields and select full width */
            padding: 8px;
            margin-top: 4px;
            margin-bottom: 8px;
            box-sizing: border-box;
        }
    </style>
</head>
<body>
   
        <form action="/addPersonal" method="POST" enctype="multipart/form-data">
            <div>
                <li>
                    <label for="image">Select Image</label>
                    <input type="file" name="image">
                </li>
                <li>
                    <label for="firstname">First Name</label>
                    <input type="text" id="firstname" name="firstname">
                </li>
                <li>
                    <label for="lastname">Last Name</label>
                    <input type="text" id="lastname" name="lastname">
                </li>
                <li>
                    <label for="dateofbirth">Date of Birth</label>
                    <input type="date" id="dateofbirth" name="dateofbirth">
                </li>
                <li>
                    <label for="weight">Weight</label>
                    <input type="text" id="weight" name="weight">
                </li>
                <li>
                    <label for="height">Height</label>
                    <input type="text" id="height" name="height">
                </li>
                <li>
                    <label>Select User</label>
                    <select id="infoItem" name="userId">
                        <% info.forEach(user => { %>
                            <option value="<%= user._id %>"><%= user.name %></option>
                        <% }); %>
                    </select>                    
                </li>
                <li>
                    <button type="submit">Add</button>
                </li>
            </div>
            <a href="/get-personals" >Personal Info</a>
        </form>
   
</body>
</html>
